// arrys, objetos
let meta = {
    value: 'Ler um livro por mes',
    checked: false,
    log: (info) => {
        console.log(info)
    }
}

meta.value = "nao é mais um ler livro"
meta.log(meta.value)

// //Function  //arrow function
// const criaMeta = () => {}

// function criarMeta() {}
